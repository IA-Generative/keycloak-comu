apiVersion: v1
kind: Service
metadata:
  name: {{ include "keycloak-comu.fullname" . }}
  labels:
    {{- include "keycloak-comu.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "keycloak-comu.selectorLabels" . | nindent 4 }}
