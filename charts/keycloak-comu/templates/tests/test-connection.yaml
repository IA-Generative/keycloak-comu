apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "keycloak-comu.fullname" . }}-test-connection"
  labels:
    {{- include "keycloak-comu.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "keycloak-comu.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
